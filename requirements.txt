# Declared here only when this plugin imports it directly.
#
# Packages CALDERA core pins with == take a >= floor here, never a pin, so
# a plugin never contests a core-owned version. Everything below is either
# core-owned (floor) or plugin-only (unpinned).
#
# Three things pip cannot do, all needed for a working install:
#   python -m spacy download en_core_web_lg
#   npm --prefix plugins/magma install && npm --prefix plugins/magma run build
#   poppler, for pdftotext, without which PDF reports cannot be ingested:
#     brew install poppler        (macOS)
#     apt install poppler-utils   (Debian/Ubuntu)

# Core-owned: floors only.
aiohttp>=3.14.1
aiohttp-jinja2>=1.5.1
jinja2>=3.1.6
lxml>=6.1.0
PyYAML>=6.0

# Plugin-only.
aiofiles
beautifulsoup4
dspy
litellm
mcp>=1.9,<2
mlflow
numpy
psutil
pydantic
python-dotenv
rapidfuzz
requests
spacy
trafilatura
